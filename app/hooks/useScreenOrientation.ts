/**
 * Orientation is now handled through app.json configuration.
 * This hook is kept for backward compatibility but does nothing.
 */
export const useScreenOrientation = () => {
  // No-op since orientation is handled by app.json
};
